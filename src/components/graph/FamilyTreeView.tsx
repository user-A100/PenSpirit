// 家族树（M5-T5 实装）：按家族边自动排谱系。此壳先行挂入 tab，
// 保证 GraphView 结构稳定。
export function FamilyTreeView() {
  return (
    <div data-testid="graph-tree-placeholder" className="flex h-full items-center justify-center text-xs text-[color:var(--text-faint)]">
      家族树将于下一任务实装
    </div>
  );
}
