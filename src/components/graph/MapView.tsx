// 世界地图（M5-T6 实装）：图片底图 + 百分比 pin。此壳先行挂入 tab。
export function MapView() {
  return (
    <div data-testid="graph-map-placeholder" className="flex h-full items-center justify-center text-xs text-[color:var(--text-faint)]">
      世界地图将于后续任务实装
    </div>
  );
}
